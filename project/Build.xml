<xml>

   <!--
      Build with:
         -Diphonesim              -> build i386 version
         -Diphoneos               -> build armv6
         -Diphoneos -DHXCPP_ARMV7 -> build armv7
         -Dandroid                -> build android armv5
         -Dandroid  -DHXCPP_ARMV7 -> build android armv7
         -Dandroid  -DHXCPP_X86   -> build android x86
         - no defines             -> build current OS
         -DHXCPP_M64              -> build current OS, 64 but version
         -Ddebug                  -> Used in combination with above for debug
   -->

<!-- Configure HXCPP -->

      <!-- Require Android 2.3+ -->
   <set name="PLATFORM" value="android-10" if="android" />
   <set name="PLATFORM" value="android-14" if="HXCPP_X86" />
   <set name="HXCPP_CPP11" value="1" />

<!-- Build set up -->

      <!-- included to import existing defines and built in defined values -->
   <include name="${HXCPP}/build-tool/BuildCommon.xml"/>
      <!-- consistency, ios is a better define than iphone and should always be used in lumen instead -->
   <set name="ios" value="1" if="iphone" />

      <!-- where to build from -->
   <!-- <set name="LUMEN_ROOT" value="${haxelib:lumen}/project/" /> -->
   <set name="LUMEN_ROOT" value="" />
      <!-- paths -->
   <set name="SRC_DIR" value="${LUMEN_ROOT}src" />
   <set name="INC_DIR" value="${LUMEN_ROOT}include" />

   <set name="NATIVE_TOOLKIT_PATH" value="${LUMEN_ROOT}src/libs" />

   <set name="LUMEN_INC_DIR" value="${LUMEN_ROOT}../include" />
   <set name="OUT_DIR" value="../ndll" />
      <!-- lib values -->
   <set name="LIBNAME" value="lumen" />
   <set name="SLIBEXT" value=".lib" if="windows"/>
   <set name="SLIBEXT" value=".a" unless="windows"/>
   <set name="SHLIBEXT" value=".dll" if="windows"/>
   <set name="SHLIBEXT" value=".so" unless="windows"/>
   <set name="SHLIBEXT" value=".dylib" if="mac || ios"/>
   <set name="DEBUGEXTRA" value="" />

   <set name="PRELIB" value="lib/${BINDIR}/${LIBPREFIX}" />
   <set name="POSTLIB" value="${LIBEXTRA}${SLIBEXT}" />
   <set name="POSTSHAREDLIB" value="${LIBEXTRA}${SHLIBEXT}" />

<!-- Build configs -->


<!-- Core lumen configs -->

   <set name="LUMEN_CORE_LOOP"   value="1"     unless="ios"/>
      <!-- are we using OpenGL bindings? -->
   <set name="LUMEN_USE_OPENGL"  value="1"/>
      <!-- are we using OpenAL bindings? -->
   <set name="LUMEN_USE_OPENAL"  value="1"/>
      <!-- are we using SDL bindings? -->
   <set name="LUMEN_USE_SDL"     value="1"/>

<!-- Porting only -->

      <!-- Don't define this unless trying to test/implement the empty platform porting helpers -->
   <!-- <set name="LUMEN_USE_EMPTY_PLATFORM" value="1" /> -->

<!-- Library configs -->

   <!-- Ideally, I want to enclose these in an "unless" section as well, so you can use command line/builds to disable them -->

      <!-- should we build SDL files -->
   <set name="LUMEN_LIB_SDL" value="1" if="LUMEN_USE_SDL"/>
   <!-- enable this to build sdl as a .a file, and then link it (instead of embedded in the core code) -->
   <!-- <set name="LUMEN_LIB_SDL_STATIC_EXTERNAL" value="1" /> -->
   <!-- enable this to build sdl as a .so/.dll file, and then link it (instead of embedded in the core code) -->
   <!-- <set name="LUMEN_LIB_SDL_SHARED_EXTERNAL" value="1" /> -->

      <!-- sometimes glew needs to be disabled because it can make null function pointers -->
   <set name="LUMEN_LIB_GLEW" value="1" unless="android || ios"/>
      <!-- always using OpenAL for main platforms by default -->
   <set name="LUMEN_LIB_OPENAL" value="1"/>
      <!-- always using stb_image for main platforms by default -->
   <set name="LUMEN_LIB_STB_IMAGE" value="1"/>
      <!-- always including lzma for main platforms by default -->
   <set name="LUMEN_LIB_LZMA" value="1" />
      <!-- always including zlib for main platforms by default, used by curl and other stuff -->
   <set name="LUMEN_LIB_ZLIB" value="1" />

      <!-- always including media libs for main platforms by default -->
   <set name="LUMEN_LIB_OGG" value="1" />
   <set name="LUMEN_LIB_THEORA" value="1" />
   <set name="LUMEN_LIB_VORBIS" value="1" />

      <!-- always including curl for main platforms by default -->
   <set name="LUMEN_LIB_CURL" value="1" />
      <!-- default to using SSL, but can be toggled -->
   <set name="NATIVE_TOOLKIT_CURL_SSL" value="1" if="LUMEN_LIB_CURL"/>

<!-- Files/Sources -->

      <!-- include linked third party libs -->
   <include name="${NATIVE_TOOLKIT_PATH}/glew/files.xml"             if="LUMEN_LIB_GLEW" />
   <include name="${NATIVE_TOOLKIT_PATH}/stb_image/files.xml"        if="LUMEN_LIB_STB_IMAGE" />
   <include name="${NATIVE_TOOLKIT_PATH}/ogg/files.xml"              if="LUMEN_LIB_OGG" />
   <include name="${NATIVE_TOOLKIT_PATH}/theora/files.xml"           if="LUMEN_LIB_THEORA" />
   <include name="${NATIVE_TOOLKIT_PATH}/vorbis/files.xml"           if="LUMEN_LIB_VORBIS" />
   <include name="${NATIVE_TOOLKIT_PATH}/lzma/files.xml"             if="LUMEN_LIB_LZMA"/>
   <include name="${NATIVE_TOOLKIT_PATH}/zlib/files.xml"             if="LUMEN_LIB_ZLIB"/>
   <include name="${NATIVE_TOOLKIT_PATH}/curl/files.xml"             if="LUMEN_LIB_CURL" />
   <include name="${NATIVE_TOOLKIT_PATH}/sdl/files.xml"              if="LUMEN_LIB_SDL"/>
   <include name="${NATIVE_TOOLKIT_PATH}/openal-android/files.xml"   if="android"/>
   <include name="${NATIVE_TOOLKIT_PATH}/openal/files.xml"           unless="mac || ios || android"/>

      <!-- lumen build files -->
   <files id="lumen">

         <!-- include paths -->
      <compilerflag value="-I${LUMEN_INC_DIR}"/>
      <compilerflag value="-I${INC_DIR}"/>

      <compilerflag value="-I${NATIVE_TOOLKIT_PATH}/glew/include/"/>
      <compilerflag value="-I${NATIVE_TOOLKIT_PATH}/stb_image/include/"/>
      <compilerflag value="-I${NATIVE_TOOLKIT_PATH}/ogg/include/"/>
      <compilerflag value="-I${NATIVE_TOOLKIT_PATH}/vorbis/include/"/>
      <compilerflag value="-I${NATIVE_TOOLKIT_PATH}/theora/include/"/>
      <compilerflag value="-I${NATIVE_TOOLKIT_PATH}/lzma/include/"/>
      <compilerflag value="-I${NATIVE_TOOLKIT_PATH}/zlib/include/"/>
      <compilerflag value="-I${NATIVE_TOOLKIT_PATH}/curl/include/"/>
      <compilerflag value="-I${NATIVE_TOOLKIT_PATH}/openal/include/"/>
      <compilerflag value="-I${NATIVE_TOOLKIT_PATH}/openal/OpenAL32/include/"/>

         <!-- c++ defines -->
      <compilerflag value="-DHX_LUMEN"  />
      <compilerflag value="-DLUMEN_CORE_LOOP"         if="LUMEN_CORE_LOOP"/>
      <compilerflag value="-D__IPHONEOS__"            if="ios"/>
      <compilerflag value="-Wno-conversion"           if="linux"/>

   <!-- lumen sources -->

         <!-- bindings -->
      <file name="${SRC_DIR}/hx_bindings.cpp" />
         <!-- assets -->
      <file name="${SRC_DIR}/assets/lumen_assets_image.cpp" />
      <file name="${SRC_DIR}/assets/lumen_assets_audio.cpp" />
         <!-- common utils -->
      <file name="${SRC_DIR}/common/ByteArray.cpp" />
         <!-- lumen core -->
      <file name="${SRC_DIR}/lumen/lumen_timestamp.cpp" />

   <!-- core options -->

      <section if="LUMEN_USE_OPENGL">

         <compilerflag value="-DLUMEN_USE_OPENGL" />
         <compilerflag value="-DGL_GLEXT_PROTOTYPES"     if="android"/>
         <compilerflag value="-DLUMEN_GLES"              if="android || ios"/>

         <file name="${SRC_DIR}/render/opengl/lumen_render_opengl.cpp" />

      </section>

      <section if="LUMEN_USE_OPENAL">

         <compilerflag value="-DLUMEN_USE_OPENAL" />

         <file name="${SRC_DIR}/audio/openal/lumen_audio_openal.cpp" />

      </section>

      <section if="LUMEN_USE_SDL">

         <compilerflag value="-DLUMEN_USE_SDL" />

            <!-- per platform configs preconfigured -->
        <compilerflag value="-I${NATIVE_TOOLKIT_PATH}/sdl/include/"/>
        <compilerflag value="-I${NATIVE_TOOLKIT_PATH}/sdl/include/configs/default/"    unless="windows || mac || linux"/>
        <compilerflag value="-I${NATIVE_TOOLKIT_PATH}/sdl/include/configs/linux/"      if="linux"/>
        <compilerflag value="-I${NATIVE_TOOLKIT_PATH}/sdl/include/configs/windows/"    if="windows"/>
        <compilerflag value="-I${NATIVE_TOOLKIT_PATH}/sdl/include/configs/mac/"        if="mac"/>

        <file name="${SRC_DIR}/lumen/sdl2/lumen_core_sdl2.cpp" />
        <file name="${SRC_DIR}/io/sdl2/lumen_io_sdl2.cpp" />
        <file name="${SRC_DIR}/window/sdl2/lumen_window_sdl2.cpp"/>
        <file name="${SRC_DIR}/input/sdl2/lumen_input_sdl2.cpp"/>

      </section>

   <!-- Porting specific helper only -->

   <section if="LUMEN_USE_EMPTY_PLATFORM">

      <compilerflag value="-DLUMEN_USE_EMPTY_PLATFORM" />

      <file name="${SRC_DIR}/lumen/empty/lumen_empty_platform.cpp"/>

   </section>

   <!-- Platform specific sources -->

      <section if="mac">
         <file name="${SRC_DIR}/platform/mac/lumen_mac.mm"/>
      </section>

      <section if="linux">
         <file name="${SRC_DIR}/platform/linux/lumen_linux.cpp"/>
      </section>

      <section if="windows">
         <file name="${SRC_DIR}/platform/windows/lumen_windows.cpp"/>
      </section>

      <section if="android">
         <file name="${SRC_DIR}/platform/android/lumen_android.cpp"/>
      </section>

      <section if="ios">
         <file name="${SRC_DIR}/platform/ios/lumen_ios.mm"/>
      </section>

   <!-- Library specific flags -->

      <section if="LUMEN_LIB_CURL">
         <include name="${NATIVE_TOOLKIT_PATH}/curl/defines.xml" />
      </section>

      <section if="LUMEN_LIB_GLEW">
         <include name="${NATIVE_TOOLKIT_PATH}/glew/defines.xml" />
      </section>

      <section if="LUMEN_LIB_LZMA">
         <include name="${NATIVE_TOOLKIT_PATH}/lzma/defines.xml" />
      </section>

      <section if="LUMEN_LIB_OGG">
         <include name="${NATIVE_TOOLKIT_PATH}/ogg/defines.xml" />
      </section>

      <section if="LUMEN_LIB_OPENAL">

         <include name="${NATIVE_TOOLKIT_PATH}/openal-android/defines.xml" if="android"/>
         <include name="${NATIVE_TOOLKIT_PATH}/openal/defines.xml" unless="mac || ios || android"/>

      </section>

      <section if="LUMEN_LIB_SDL">
         <include name="${NATIVE_TOOLKIT_PATH}/sdl/defines.xml" />
      </section>

      <section if="LUMEN_LIB_STB_IMAGE">
         <include name="${NATIVE_TOOLKIT_PATH}/stb_image/defines.xml" />
      </section>

      <section if="LUMEN_LIB_THEORA">
         <include name="${NATIVE_TOOLKIT_PATH}/theora/defines.xml" />
      </section>

      <section if="LUMEN_LIB_VORBIS">
         <include name="${NATIVE_TOOLKIT_PATH}/vorbis/defines.xml" />
      </section>

      <section if="LUMEN_LIB_ZLIB">
         <include name="${NATIVE_TOOLKIT_PATH}/zlib/defines.xml" />
      </section>

   </files>



<!-- Targets -->



   <target id="lumen" output="${LIBPREFIX}${LIBNAME}${MSVC_LIB_VERSION}${DEBUGEXTRA}${LIBEXTRA}" tool="linker" toolid="${STD_MODULE_LINK}">

      <outdir name="${OUT_DIR}/${BINDIR}" />

<!-- Build the libs -->

      <files id="native-toolkit-glew"           if="LUMEN_LIB_GLEW"/>
      <files id="native-toolkit-curl"           if="LUMEN_LIB_CURL"/>
      <files id="native-toolkit-lzma"           if="LUMEN_LIB_LZMA"/>
      <files id="native-toolkit-ogg"            if="LUMEN_LIB_OGG"/>
      <files id="native-toolkit-stb_image"      if="LUMEN_LIB_STB_IMAGE"/>
      <files id="native-toolkit-theora"         if="LUMEN_LIB_THEORA"/>
      <files id="native-toolkit-vorbis"         if="LUMEN_LIB_VORBIS"/>
      <files id="native-toolkit-zlib"           if="LUMEN_LIB_ZLIB"/>
      <files id="native-toolkit-sdl"            if="LUMEN_LIB_SDL"      unless="LUMEN_LIB_SDL_STATIC_EXTERNAL || LUMEN_LIB_SDL_SHARED_EXTERNAL"/>

      <section if="LUMEN_LIB_OPENAL">
         <files id="native-toolkit-openal-android" if="android"/>
            <!-- mac and iOS have OpenAL.framework, and rather rely on system version than embedded one -->
         <files id="native-toolkit-openal" unless="mac || ios || android"/>
      </section>

<!-- Build the lumen library -->

         <!-- build the lumen sources -->
      <files id="lumen"/>

<!-- Linking flags and libraries -->

      <section if="LUMEN_LIB_SDL">
         <lib name="${PRELIB}SDL2${POSTLIB}"       if="LUMEN_LIB_SDL_STATIC_EXTERNAL"/>
         <lib name="${PRELIB}SDL2${POSTSHAREDLIB}" if="LUMEN_LIB_SDL_SHARED_EXTERNAL"/>
      </section>

      <section unless="static_link">

         <ext value=".so" />
         <ext value=".ndll" if="windows || mac || linux" />

         <section if="windows">
            <lib name="gdi32.lib" />
            <lib name="opengl32.lib" />
            <lib name="user32.lib" />
            <lib name="kernel32.lib" />
            <lib name="advapi32.lib" />
            <lib name="winmm.lib" />
            <lib name="imm32.lib"  />
            <lib name="ole32.lib" />
            <lib name="oleaut32.lib" />
            <lib name="version.lib" />
            <lib name="ws2_32.lib"  />
            <lib name="wldap32.lib" />
            <lib name="shell32.lib" />
            <lib name="comdlg32.lib" />
         </section> <!-- windows -->

         <section if="linux">
            <lib name="${HXCPP}/lib/${BINDIR}/liblinuxcompat.a" />
            <lib name="-lpthread" />
            <lib name="-lrt" />
            <lib name="-lGL" />/
         </section> <!-- linux -->

         <section if="mac">
            <vflag name="-l" value="iconv"/>
            <vflag name="-framework" value="IOKit" />
            <vflag name="-framework" value="Foundation" />
            <vflag name="-framework" value="CoreAudio" />
            <vflag name="-framework" value="Cocoa" />
            <vflag name="-framework" value="OpenGL" />
            <vflag name="-framework" value="AudioToolbox" />
            <vflag name="-framework" value="AudioUnit" />
            <vflag name="-framework" value="ForceFeedback" />
            <vflag name="-framework" value="Carbon" />
            <vflag name="-framework" value="AppKit" />
            <vflag name="-framework" value="OpenAL"/>
         </section> <!-- mac -->

         <section if="ios">

         </section>

         <section if="android">
            <lib name="-Wl,--undefined=Java_org_libsdl_app_SDLActivity_nativeInit" if="LUMEN_USE_SDL"/>
            <lib name="-ldl" />
            <lib name="-llog" />
            <lib name="-landroid" />
            <lib name="-lEGL" />
            <lib name="-lGLESv2" />
            <lib name="-lGLESv1_CM" />
            <lib name="-lOpenSLES" />
         </section> <!-- android -->

      </section> <!-- unless static_link -->

   </target>



   <target id="default">

         <!-- if we have sdl set to build as static but not embedded, build it -->
      <target id="libs-sdl" if="LUMEN_LIB_SDL_STATIC_EXTERNAL"/>
         <!-- if we have sdl set to build as shared, build it -->
      <target id="libs-sdl-shared" if="LUMEN_LIB_SDL_SHARED_EXTERNAL"/>
         <!--  -->
      <target id="lumen" unless="no_lumen"/>

   </target>



</xml>
