<?xml version="1.0" encoding="utf-8"?>

<extension>

<!-- defines -->

    <haxedef name="snow_html5" if="html5" />
    <haxedef name="snow_native" if="native" />
    <haxedef name="no-compilation" />

<!-- binding configs -->

    <!-- audio specific bindings -->

        <!-- native -->
            <set name="snow_audio_openal" value="1" if="native" />
            <haxedef name="snow_audio_openal" if="snow_audio_openal" />

        <!-- html5 -->
            <set name="snow_audio_howlerjs" value="1" if="html5" />
            <haxedef name="snow_audio_howlerjs" if="snow_audio_howlerjs" />

    <!-- input specific bindings -->
        <set name="snow_input_sdl" value="1" if="native" />
        <haxedef name="snow_input_sdl" if="snow_input_sdl" />

    <!-- window specific bindings -->
        <set name="snow_window_sdl" value="1" if="native" />
        <haxedef name="snow_window_sdl" if="snow_window_sdl" />

<!-- platform configs -->

        <!-- android -->
    <architecture name="armv7" if="android" />
    <dependency name="extension-api" path="templates/android-dependencies/extension-api" if="android" />

<!-- build tools configs -->

    <templatePath name="templates" />
    <sample path="samples" />

<!-- dependencies -->

    <section unless="display">

    <!-- native dependency -->

            <!-- build against hxcpp always for cpp builds -->
        <haxelib name="hxcpp" if="cpp" />
            <!-- + hxcpp ndlls -->
        <ndll name="std"    haxelib="hxcpp" if="cpp" unless="static_link"/>
        <ndll name="regexp" haxelib="hxcpp" if="cpp" unless="static_link"/>
        <ndll name="zlib"   haxelib="hxcpp" if="cpp" unless="static_link"/>
            <!-- for the tools/templates/bin files, like minify etc -->
        <haxelib name="lime-tools"/>

    <!-- html5 deps -->

        <dependency path="templates/html5-dependencies/howlerjs/howler.min.js" if="snow_audio_howlerjs"/>

    <!-- common dependency  -->

        <ndll name="snow"/>

    </section>

</extension>
