{

  project : {
    name : 'test_flow',
    version : '1.0.0',
    author : 'snowkit',

    app : {
      main : 'Main',
      output : 'bin',
      name : 'flow_test',
      package : 'org.snowkit.flow',
      codepaths : [ 'src' ],
    },

    build : {
      dependencies : {
        snow : '*',
      },
      defines : ['one=1', 'two = 2', 'some_define']
    },

    files : {
      assets : 'assets/',
    }

  },

  snow : {
    window : {}
  },

  if: {
    "mac" : {
      build: {
        defines : ['ismac']
      },
      files : {
        config : 'config.json => config.json'
      }
    },
    "!mac && some_define" : {
      build: {
        defines : ['complex']
      }
    }
  }

}